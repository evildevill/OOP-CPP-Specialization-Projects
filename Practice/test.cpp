#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    // float a;
    // cin >> a;
    // a *= a;
    // a *= a;
    // a = sqrtf (a) * sqrtf (a);
    // cout << a << endl;

    // float var;
    // var = 10;
    // var = var + var;
    // cout << var << endl;

    // float var = .1;
    // var = var + 1.;
    // var = var + 1e-2;
    // cout << var << endl;

    // int i = 10, j = 3;
    // int k = (i % j * i / 3) / (j % i - j / i);
    // cout << k << endl;

    // int i, j;
    // cin >> i;
    // j = i;
    // if (i >= 9)
    //     i += 9 - i;
    // j /= i;
    // cout << j << endl;

    // float a, b;
    // cin >> a;
    // b = a / 2;
    // if (b >= 1)
    //     a += b;
    // if (a > b)
    //     b += a;
    // cout << a + b;

}